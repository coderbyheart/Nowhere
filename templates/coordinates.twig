{% extends "base.twig" %}
{% block body %}
<header id="master" class="desktop">
    <h1>Nowhere</h1>
</header>
<div id="main">
    {% for stone in stones %}
    {% if loop.first %}
    <ul id="coordsgrid" class="draggrid nozoom">
        {% endif %}
        <li>
            <a href="/{{ lang }}/stone/{{ stone.number }}"
               title="Coordinates in {{ stone.locality }}, {{ stone.country }} where {{ stone.person }} found a stone.">
                <span class="nowrap">{{ stone.lat }}</span><br/>
                <span class="nowrap">{{ stone.lng }}</span><br/>
            </a>
        </li>
        {% if loop.last %}
    </ul>
    {% endif %}
    {% endfor %}
</div>
{% from 'macros.twig' import coordlink %}
{{ coordlink(lang, navactive) }}
{% endblock %}